server:
  port: 9000

spring:
  application:
    name: gateway-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    employee-service: /gateway/employee/**
    department-service: /gateway/department/**
  sensitive-headers:
  ignored-patterns: /*-service/**